export enum OrderStageType {
    CREATED = "CREATED",
    PACKAGED = "PACKAGED",
    PROCESSED = "PROCESSED",
    SHIPPED = "SHIPPED",
    DELIVERED = "DELIVERED"
}

export enum PricePromoType {
    FLAT = "FLAT",
    PERCENTAGE = "PERCENTAGE"
}
