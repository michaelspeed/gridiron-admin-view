overwrite: true
schema: "http://megatron.assammart.shop/"
documents: "gql/*.graphql"
generates:
  gql/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-vue-apollo
    config:
      withCompositionFunctions: false
